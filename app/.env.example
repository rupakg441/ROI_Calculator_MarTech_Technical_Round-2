# Server-only secrets: never prefix these with VITE_ or NEXT_PUBLIC_
HUBSPOT_ACCESS_TOKEN=
GOOGLE_SHEETS_WEBHOOK_URL=
RESEND_API_KEY=
NOTIFICATION_TO_EMAIL=ai-labs@hexanovate.com
EMAIL_FROM=KlearStack leads <onboarding@resend.dev>
CALENDAR_BOOKING_URL=
# Safe to expose to the browser because it is only a public booking URL.
VITE_CALENDAR_BOOKING_URL=
